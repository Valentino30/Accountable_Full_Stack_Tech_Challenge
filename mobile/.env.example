# IMPORTANT: When running the app on a physical device (like an Android phone with Expo Go),
# replace '192.168.0.x' with the current local IPv4 address of the machine
# running the backend server (e.g., your laptop).
#
# To find your machine's IP, use 'ipconfig' (Windows) or 'ifconfig' (macOS/Linux).
#
# If testing with an official Android Emulator, use 10.0.2.2 instead of the Wi-Fi IP.

EXPO_PUBLIC_API_URL="http://[YOUR_LOCAL_IP_ADDRESS]:5001/api"